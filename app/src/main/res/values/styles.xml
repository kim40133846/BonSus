<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    
    <style name="botones">
        <item name="android:layout_width">@dimen/btn_largo</item>
        <item name="android:layout_height">@dimen/btn_alto</item>
        <item name="android:scaleType">centerCrop</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <item name="android:background">@drawable/trasparencia</item>
    </style>

    <style name="botones_chicos">
        <item name="android:layout_width">@dimen/btn_chico</item>
        <item name="android:layout_height">@dimen/btn_chico</item>
        <item name="android:scaleType">centerCrop</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <item name="android:background">@drawable/trasparencia</item>
    </style>

    <style name="logo">
        <item name="android:layout_width">@dimen/logo</item>
        <item name="android:layout_height">@dimen/logo</item>
        <item name="android:scaleType">centerCrop</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <item name="android:background">@drawable/trasparencia</item>
    </style>

</resources>
